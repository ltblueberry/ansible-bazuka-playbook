server {

    listen 80;
    listen [::]:80;

    server_name _;

    # proxy to application, default port 3000
    location / {
        proxy_pass http://127.0.0.1:3000;
    }

    client_max_body_size 100M;

}